using System; 

public class Program {

  public static void Main(string[] args) {

    
    Console.WriteLine("digite o quatro valores inteiros:");
    string numero1 = Console.ReadLine();
    double N1 = double.Parse(numero1);
    string numero2 = Console.ReadLine();
    double N2 = double.Parse(numero2);
    string numero3 = Console.ReadLine();
    double N3 = double.Parse(numero3);
    string numero4 = Console.ReadLine();
    double N4 = double.Parse(numero4);

    double media = (N1+N2+N3+N4)/4;
    Console.WriteLine("media = " + media);

    Console.WriteLine("Numeros menores que a media:");
    
    if((N1) < media){
      Console.WriteLine(N1);
    }
      if((N2) < media){
      Console.WriteLine(N2);
    }
      if((N3) < media){
      Console.WriteLine(N3);
    }
      if(N4 < media){
      Console.WriteLine(N4);
    }
      
    Console.WriteLine("Numeros maiores que a media:");
    
    if(N1 > media){
      Console.WriteLine(N1);
    }
      if(N2 > media){
      Console.WriteLine(N2);
    }
    if(N3 > media){
      Console.WriteLine(N3);
    }
    if(N4 > media){
      Console.WriteLine(N4);
    }
      
    }
  }
